export const tronca = (string, l) => {
    //INSERISCI QUI IL TUO CODICE
                                            if (string.length > l) {
        return string.slice(0, l) + '...';
    }
    return string;
};
                                                                                                                                                            