export function is_anagram(arr) {
    // let angrams = new set(arr);
    // console.log(angrams);
    // return angrams;
    // return Array.from(new Set(arr));
    // return arr.filter((word, _, self) => self.filter(w => w.split('').sort().join('') === word.split('').sort().join('')).length === 1);
    
    
}

