   let uguali = arr1.length === arr2.length && arr1.every((item, index) => item === arr2[index]);

   return uguali


//    if (arr1.length === arr2.length) {
//       let uguali = arr1.every((item, index) => item === arr2[index]);

//       return uguali;

//   } 
//   else{

//       return false;
  
//    }




