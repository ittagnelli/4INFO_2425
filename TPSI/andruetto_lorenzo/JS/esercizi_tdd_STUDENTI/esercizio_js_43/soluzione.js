export const without = (l, n) => {
    //INSERISCI QUI IL TUO CODICE
    let newArray = [];
    l.forEach((_, i) => {
        if (l[i] != n)
            newArray.push(_)
    });
    return newArray;
};