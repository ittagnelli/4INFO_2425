const is_prime = (n) => {
   return Array.from(Array.ì(n).keys()).filter(e => e >= 1).reduce((acc, e) => acc && e % , true)
}
export const order = (l) => {
   //INSERISCI QUI IL TUO CODICE
   return l.filter()
};